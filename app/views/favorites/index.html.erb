pictures<p id="notice"><%= notice %></p>

<h1>Favo</h1>

<table>
  <thead>
    <tr>
      <th>Information</th>
      <th>Favo・Favo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @favorites.each do |favorite| %>
      <tr>
        <td><%= favorite.picture.content %></td>
        <td><%= favorite.user.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Back', pictures_path %>
